version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html

hooks:
  BeforeInstall:
    - location: scripts/clean.sh
      timeout: 60
      runas: root

  AfterInstall:
    - location: scripts/start_proxy.sh
      timeout: 60
      runas: ec2-user